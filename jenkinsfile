@Library("sharedlib") _
pipeline {
    agent any

    stages {
        stage('gitcheckout') {
            steps {
                git() 
      }
        }
      stage('junit test') {
            steps {
                test()
            }
        }
    }
}

